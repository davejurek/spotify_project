import styled from 'styled-components';

const Wrap = styled.nav`
    width: 241px;
    background-color: #000000;
`;

const NavPanel = () => {
    return <Wrap>Menu</Wrap>
};

export default NavPanel;